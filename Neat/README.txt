each file is named after the approach.
there is no main file. to train the model,
run any file regarding its name.
to evaluate, run pesq_stoi.m file in ./evaluation.
to test the model, run test_model_rnn if it was an RNN model or
test_model_context if it was a context model.
to create data run > ./create data/create_data
you need to fix directories for each code.